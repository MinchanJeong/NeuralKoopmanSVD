# .env.example
# Copy to .env and modify

# === Paths ===
# Defaults are ./data and ./results if not set
DATA_DIR=./data
RESULT_DIR=./results

# === Hardware ===
# Which GPUs to make visible
CUDA_VISIBLE_DEVICES="0,1"
NUM_GPUS=2

# === Optional Overrides ===
# RAW_DIR=/path/to/raw/chignolin
# PROCESSED_DIR=/path/to/processed/chignolin
